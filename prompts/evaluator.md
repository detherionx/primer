# Response evaluator prompt (Phase 3+).

Until a live model is wired, evaluation is deterministic against curriculum expected answers and misconception regexes in `src/core/evaluate.ts`.

If you change classification behavior, update evals first.
